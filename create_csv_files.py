import csv\nimport sqlite3\n\ndef export_table_to_csv(table_name, csv_file_name):\n    conn = sqlite3.connect('LittleLemonDB.db')  # Connect to the SQLite database\n    cursor = conn.cursor()\n    # Fetch the data from the specified table\n    cursor.execute(f'SELECT * FROM {table_name}')\n    rows = cursor.fetchall()\n    # Get column headers\n    column_headers = [description[0] for description in cursor.description]  # Fetch column headers\n    # Write data to CSV\n    with open(csv_file_name, mode='w', newline='') as file:\n        writer = csv.writer(file)\n        writer.writerow(column_headers)  # Write headers\n        writer.writerows(rows)  # Write data rows\n\nif __name__ == '__main__':\n    export_table_to_csv('Customers', 'customers.csv')\n    export_table_to_csv('Bookings', 'bookings.csv')\n    export_table_to_csv('Orders', 'orders.csv')\n